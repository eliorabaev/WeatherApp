<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bootstrap Example</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" type="text/css" href="styles/main.css">
</head>
<body>
    <div class="container">
        <form id="myForm" method="get" action="/get-weather">
            <div class="row d-flex justify-content-center py-5">
                <div class="col-md-6">
                    <div class="form">
                        <i class="fa fa-search"></i>
                        <input type="text" class="form-control form-input" placeholder="Search for City/Town..." name="city" id="cityInput">
                    </div>
                    <% if(locals.error) { %>
                    <h6 class="warning"><%= error %></h6>
                    <% } %>
                 </div>
            </div>
        </form>
        <section>
          <div class="container py-5">
              <div class="row d-flex justify-content-center align-items-center">
                  <div class="col-md-8 col-lg-6 col-xl-4">
                      <div class="card" style="color: #4B515D; border-radius: 35px;">
                          <div class="card-body p-4">
                              <% if (locals.error) { %>
                                  <div class="alert alert-danger" role="alert">
                                      <%= error %>
                                  </div>
                              <% } else { %>
                                  <div class="d-flex">
                                      <h6 class="flex-grow-1"><% if (locals.flag) { %>Estimated location: <% } %><%= location %></h6>
                                      <h6><%= localtime %></h6>
                                  </div>
                                  <div class="d-flex flex-column text-center mt-5 mb-4">
                                      <h6 class="display-4 mb-0 font-weight-bold" style="color: #1C2331;"><%= temp %>°C </h6>
                                      <span class="small" style="color: #868B94"><%= condition %></span>
                                  </div>
                                  <div class="d-flex align-items-center">
                                      <div class="flex-grow-1" style="font-size: 1rem;">
                                          <div><i class="fas fa-wind fa-fw" style="color: #868B94;"></i> <span class="ms-1"> <%= wind %> km/h </span></div>
                                          <div><i class="fas fa-tint fa-fw" style="color: #868B94;"></i> <span class="ms-1"> <%= humidity %>% </span></div>
                                          <div><i class="fas fa-sun fa-fw" style="color: #868B94;"></i> <span class="ms-1"> <%= uv %>h </span></div>
                                      </div>
                                      <div>
                                          <img src="<%= icon %>" width="100px">
                                      </div>
                                  </div>
                              <% } %>
                          </div>
                      </div>
                      <form method="get" action="/">
                        <div class="d-flex justify-content-center mt-3">
                            <button type="submit" class="btn btn-outline-dark">Back to my location</button>
                        </div>
                      </form>
                  </div>
              </div>
          </div>
      </section>      
    </div>
  
    <!-- jQuery -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <!-- Bootstrap Bundle JS (includes Popper) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
